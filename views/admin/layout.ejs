<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Trang Quản Trị</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="/css/tramsac.css" />
    <style>
        .wrapper {
            width: 100%;
            padding: 10px 0; /*Thêm padding để tạo khoảng cách*/
            background-color: #f8f9fa; /* Màu nền cho menu */

        }
        .wrapper ul {
            list-style: none; /* Loại bỏ dấu chấm tròn */
            margin: 0;
            padding: 0;
            display: flex; /* Hiển thị các mục theo chiều ngang */
            justify-content: center;
        }
        .wrapper li {
            margin: 0 15px; /* Khoảng cách giữa các mục */
        }
        .wrapper a {
            text-decoration: none; /* Loại bỏ gạch chân */
            color: #333; /* Màu chữ mặc định */
            padding: 8px 16px;  /*Thêm padding cho phần tử a */
            border-radius: 5px; /* Bo tròn góc */
            transition: background-color 0.3s ease; /* Hiệu ứng mượt mà khi hover */
        }

        /* Tô đậm trang hiện tại */
        .wrapper a.active {
            font-weight: bold;
            background-color: #007bff; /* Màu nền khi active */
            color: white; /*Màu chữ khi active */

        }

        .wrapper a:hover {
            background-color: #007bff; /* Màu nền khi hover */
            color: white; /*Màu chữ khi hover */

        }

          .account-button {
            position: absolute;
            top: 10px;
            right: 10px;
            padding: 10px 15px;
            background-color: red;
            color: #fff;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            font-size: 14px;
            font-family: Arial, sans-serif;
            cursor: pointer;
            z-index: 1000; /* Đặt z-index cao hơn để hiển thị trên cùng */
            transition: background-color 0.3s ease;
          }
        

    </style>
  </head>
  <body>
    <form action="/account/logout">
      <button id="accountButton" class="account-button" type="submit">Đăng Xuất</button>
    </form>
    <div class="wrapper">
      <h2>TRANG QUẢN TRỊ</h2>
      <ul>  
        <li><a href="/admin/tramsac" <% if (body === 'Trạm Sạc') { %> class="active" <% } %>>Trạm Sạc</a></li>
        <li><a href="/admin/hangxe" <% if (body === 'Hãng Xe') { %> class="active" <% } %>>Hãng Xe</a></li>
        <li><a href="/admin/xe" <% if (body === 'Xe') { %> class="active" <% } %>>Xe</a></li>
        <li><a href="/admin/chuansac" <% if (body === 'Chuẩn Sạc') { %> class="active" <% } %>>Chuẩn Sạc</a></li>
      </ul>
    </div>
    <% if (messages && Object.keys(messages).length > 0) {
      %>
      <div id="flash-messages" class="container mt-4">
        <% for (const type in messages) { %> <% messages[type].forEach(message => { %>
        <div class="alert alert-<%= type %>"><%= message %></div>
        <% }); %> <% } %>
      </div>
      <% } %>
  </body>

</html>
